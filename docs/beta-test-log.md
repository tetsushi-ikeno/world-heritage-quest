# 娘向けベータ版 実機テストログ

更新日: 2026-08-22

## 2026-08-22 Golden Path 1回目

### 確認できたこと

- チュートリアルから全国マップへ進める
- 全国マップを移動できる
- `?` に接触して発見できる
- 発見後に研究センターへ入れる
- 研究センター内の基本体験は期待どおり動作する
- センターから全国マップへ戻れる

### 不具合1: ピラミトン表示の先祖帰り

**現象**

ベータのチュートリアルで、確定済みデザインではなく以前の乱れたピラミトン表示になっていた。

**原因**

ベータが参照している `tutorial-lab-final.html` は画像シート切り出し版を使用していたが、その後に確定した `tutorial-design-baseline.html` の approved SVG Piramiton が反映されていなかった。

**対応**

ベータ専用 `beta-hotfix.js` から approved SVG Piramiton を適用する。チュートリアル本体の画面構成・文章・進行は変更しない。

**状態**: 修正済み・実機確認OK

### 不具合2: 発見後も `?` が残り、施設アイコンがずれる

**現象**

発見してセンターから戻ると、元の `?` が残り、施設アイコンが別の位置へ表示された。

**1回目の対応と結果**

外側の `area-map-game.html` 上にある施設DOMを半マス補正して `?` を覆う方式を試したが、実機では施設と `?` が大きく離れたままで改善しなかった。

原因は単純な14pxの補正だけではなく、承認済み6倍マップのcanvasカメラと、その外側に重ねている施設DOMが別レイヤーで動いていることにあると判断した。

**2回目の対応**

承認済み `japan-6x-map-lab.html` 本体は変更せず、ベータ専用 `beta-hotfix.js` から6倍マップ内部の `.camera` に施設レイヤーを注入する方式へ変更。

- `?` と同じ `screenCenter + (siteFine - player - 0.5) * TILE` の式で施設位置を計算
- 発見済み施設は `?` と同じカメラ内へ描画
- 施設背面の草原色マスクで元の `?` を隠す
- 旧来の外側施設レイヤーはベータ時のみ非表示
- 地形・縮尺・27遺産の座標・移動処理は変更しない

**状態**: 2回目修正済み・実機再確認待ち

## 次回確認

1. 発見済みの知床で、`?` が消えて同じ位置に施設アイコン＋「知床」ラベルが表示されること
2. 移動しても施設アイコンが地図と一緒にずれずに追従すること
3. 施設から一度離れて再接触し、発見演出なしで再入場できること
4. 別の未発見 `?` でも、発見→退出後に同じ置き換えが成立すること
